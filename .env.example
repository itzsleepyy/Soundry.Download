# Environment Variables

## Required
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

## Optional - Worker Scaling
# Number of concurrent jobs per worker instance (default: 10)
# I/O-bound tasks can handle higher concurrency
WORKER_CONCURRENCY=10

## Optional - YouTube Cookies (Bot Detection Fallback)
# Export cookies from YouTube using browser extension
# Paste entire Netscape cookie file content as value
# YOUTUBE_COOKIE_1="# Netscape HTTP Cookie File..."
# YOUTUBE_COOKIE_2="# Additional cookie for rotation..."
