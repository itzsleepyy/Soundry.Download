# Environment Variables

## Required
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

## Optional - Worker Scaling
# Number of concurrent jobs per worker instance (default: 10)
# I/O-bound tasks can handle higher concurrency
WORKER_CONCURRENCY=10



## Residential Proxy Configuration (Recommended)
# Comma-separated list of proxy URLs (HTTP or SOCKS5)
# Format: http://user:pass@host:port or socks5://user:pass@host:port
# Tested with Oxylabs residential proxies
# PROXY_URLS="http://customer-user:pass@pr.oxylabs.io:7777"

## Cookie Service Configuration (Phase B - Future)
# AUTO_COOKIE_REFRESH=true
# COOKIE_REFRESH_DAYS=14

